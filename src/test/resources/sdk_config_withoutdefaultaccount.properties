acct2.UserName = certuser_biz_api1.paypal.com
acct2.Password = D6JNKKULHN3G5B8A
acct2.CertKey=password
# replace the below path with absolute certificate path.
acct2.CertPath=sdk-cert.p12
acct2.AppId=APP-80W284485P519543T

# Connection Information
http.ConnectionTimeOut=5000
http.Retry=2
http.ReadTimeOut=30000
http.MaxConnection=100
http.IPAddress=127.0.0.1

# HTTP Proxy configuration
# If you are using proxy set http.UseProxy to true and replace the following values with your proxy parameters
http.ProxyPort=8080
http.ProxyHost=127.0.0.1
http.UseProxy=false
http.ProxyUserName=null
http.ProxyPassword=null

#Set this property to true if you are using the PayPal SDK within a Google App Engine java app
http.GoogleAppEngine = false

# Service Configuration
service.EndPoint=https://svcs.sandbox.paypal.com/
service.RedirectURL=https://www.sandbox.paypal.com/webscr&cmd=
service.DevCentralURL=https://developer.paypal.com
service.IPNEndpoint=https://ipnbp.sandbox.paypal.com/cgi-bin/webscr


#Sandbox Details
sandbox.EmailAddress=Platform.sdk.seller@gmail.com

# Multi end-point configuration, while using multiple SDKs in combination, like Merchant and Permissions etc..
# configure the endpoints for the respective services by appending the appropriate 'Port Name' retrieved from the WSDL 
# with the 'service.EndPoint' key parameter. The SDK core looks for a more specific endpoint (represented by the the 
# service.EndPoint.<PortName> property) if one is present for a particular service and pick the same, if 
# one such entry is not found uses the default key 'service.EndPoint'. Typical services and their Port Names are given
# for reference. Port names are taken from the respective WSDLs.

# Merchant/Button Manager Service (3 Token)
# service.EndPoint.PayPalAPI=https://api-3t.sandbox.paypal.com/2.0
# service.EndPoint.PayPalAPIAA=https://api-3t.sandbox.paypal.com/2.0

# Merchant/Button Manager Service (Certificate)
# service.EndPoint.PayPalAPI=https://api.sandbox.paypal.com/2.0
# service.EndPoint.PayPalAPIAA=https://api.sandbox.paypal.com/2.0

# AdaptiveAccounts Platform Service
# service.EndPoint.AdaptiveAccountsSOAP11port_http=https://svcs.sandbox.paypal.com/

# AdaptivePayments Platform Service
# service.EndPoint.AdaptivePaymentsSOAP11_http=https://svcs.sandbox.paypal.com/

# Invoice Platform Service
# service.EndPoint.InvoiceSOAP11_http=https://svcs.sandbox.paypal.com/

# Permissions Platform Service
# service.EndPoint.PermissionsSOAP11port_http=https://svcs.sandbox.paypal.com/
